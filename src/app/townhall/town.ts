/*import { Injectable } from '@angular/core';
import { Building } from '../building';
import { Armory } from '../armory';
import { Market } from '../market';
import { Resources } from '../resources/resources';
import { Collector } from '../collectors/Collector';
import { Townhall } from '../townhall';
//import { Quarter } from '../quarter';
import { GoldService } from '../resources/GoldService';
import { MeatService } from '../resources/MeatService';
import { WoodService } from '../resources/WoodService';

@Injectable()
export class Town {
  armory: Armory;
  market: Market;
  townhall: Townhall;
  collectors: Collector[];
  //quarter: Quarter;

  constructor() {
    this.armory = new Armory(1, 400);
    this.market = new Market(1, 600, [0.5, 0.5], [0.5, 0.5], 0.1);
    this.townhall = new Townhall(1, 800, 2000, 100, 1000, 50);
    this.collectors = [new Collector(1, 200, 'woodCutterHouse', 1, 1),
                       new Collector(1, 200, 'butcherHouse', 1, 1),
                       new Collector(1, 200, 'goldMine', 1, 1)]
    //this.quarter
  }

}*/
